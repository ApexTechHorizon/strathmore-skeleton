/**
 * U-CS 3 — the agreed shapes.
 *
 * Fixed by the Team Integration Map v0.1. Changing them breaks: career-path (U-CS 1) and micro-learning (U-CS 8)
 */

export class SkillsGapRequestDto {
  // in : { profile, targetRole }
}

export class SkillsGapResponseDto {
  // out: { have[], missing[{skill, confidence}], readiness }
}
