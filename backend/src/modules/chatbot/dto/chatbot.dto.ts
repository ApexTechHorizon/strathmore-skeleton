/**
 * U-CS 4 — the agreed shapes.
 *
 * Fixed by the Team Integration Map v0.1. Changing them breaks: nobody directly
 */

export class ChatbotRequestDto {
  // in : { question, userId }
}

export class ChatbotResponseDto {
  // out: { answer, citations[], handover, handoverReason }
}
